({
  init: function( component, event, helper ){
    helper.init( component, event, helper );
  },
  handleStepChange: function( component, event, helper ){
    helper.handleStepChange( component, event, helper );
  },
  toggleNextButton: function( component, event, helper ){
    helper.toggleNextButton( component, event, helper );
  }
})